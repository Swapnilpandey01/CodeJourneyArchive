#include<stdio.h>

/*how to find area of square using c language*/
int main(){

    int side;
    printf("Enter the length of side of square:");
    scanf("%d", &side);
    printf("The area of square is: %d", side * side);
    
    return 0;
}